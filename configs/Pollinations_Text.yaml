# Configuration for Pollinations_Text
# Generated dynamically from CSV data
# Tier: community | Models: /models

provider:
  name: "Pollinations_Text"
  tier: "community"
  base_url: "https://text.pollinations.ai/"
  api_key: ""

models:
  source: "/models"
  dynamic_loading: true
  
configuration:
  timeout: "30s"
  max_retries: 3
  
rate_limiting:
  enabled: true
  requests_per_minute: 10
  
capabilities:
  - chat
  - creative

metadata:
  description: "Free to use with a 10 requests per minute rate limit"
  auto_generated: true
  csv_source: "providers.csv"
  generated_at: "2025-08-22 20:45:00"
  
# Tier-specific configurations
community_features:
  free_tier: true
  rate_limited: true
  best_effort: true